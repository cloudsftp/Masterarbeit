\sectionframe{The Halved Model}
\section{Halved Model}

\begin{frame}{Lifting the Model}
    \todo{visual explanation of the halved model}
\end{frame}

\begin{frame}{Definition of the Halved Minimal Reproducing Model}
    \vspace{-3.0em}
    \begin{align}
        x \mapsto g(x) \mod \frac{1}{2}
    \end{align}

    \begin{align}
        g(x) & = \begin{cases}
                     l(x) = a_L \cdot x^2 + b_L \cdot x + c_L & \text{ if } x < \frac{1}{4} \\
                     r(x) = b_R \cdot x + c_R                 & \text{ else}
                 \end{cases}
    \end{align}
    \todo{picture of halved model function}
\end{frame}

\begin{frame}{Period-adding in the Halved Model}
    \todo{pic of horizontal period-adding in the halved model}
    \todo{tree of symbolic sequences in the halved model}
\end{frame}

\begin{frame}{Translating Symbolic Sequences}
    \todo{algo for full -> halved}
    \todo{algo for halved -> full}
    \todo{maybe skip if not enough time}
\end{frame}

\begin{frame}
    \todo{
        theorems: \\
        - when does a cycle manifest as a singular or two coexisting cycles \\
        - children of singular + coex. / two singular \\
        - rules for combining cycles in the full model (not concatenation) \\
        - rules for combining rotation-like numbers
    }
\end{frame}